<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <meta charset="utf-8">
    <title>Index of Companies</title>
    <link rel="stylesheet" href="/css/app.css">
</head>

<body>
    <h1>All The Companies:</h1>
    <nav>
        <a href="/companies/new">Create a New Company</a>
    </nav>
    <ul>
        <% for (i in companies) { %>
        <li>
            <%= companies[i].name %>,
            <%= companies[i].logo %>,
            <%= companies[i].address %>,
            <%= companies[i].city %>,
            <%= companies[i].telephone %>,
            <%= companies[i].createdAt %>,
            <%= companies[i].updatedAt %>
            <% for (d in companies[i].drivers) { %>
        <li>
            <%= companies[i].driver[d].name %>
            <%= companies[i].driver[d].age %>
            <%= companies[i].driver[d].image %>
        </li>
        <% for (c in companies[i].cars) { %>
            <li>
        <%= companies[i].car[d].name %>
        <%= companies[i].car[d].model %>
        <%= companies[i].car[d].year %>
        <%= companies[i].car[d].image %>
        </li>
        </li>
        <a href="/companies/<%= companies[i]._id %>">SHOW</a>
        <a href="/companies/<%= companies[i]._id %>/edit">EDIT</a>
        <form action="/companies/<%= companies[i]._id %>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE" />
            <% } %>
    </ul>
</body>

</html>